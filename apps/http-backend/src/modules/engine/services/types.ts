export interface IRSIresult {
    value: number;
    score: number;
}

export interface ISMAresult {
    value:number,
    score:number
}

export interface IVolumeresult {
    value:number,
    score:number
}